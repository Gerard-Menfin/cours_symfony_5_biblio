{{ form_start(form, {"attr" : {"class": "frm" }}) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary"><i class="fa fa-save"></i> {{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
