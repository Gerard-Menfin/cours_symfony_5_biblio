<div class="d-flex justify-content-between pb-5">
    <a href="{{ path('app_admin_' ~ entite ~ '_index') }}" class="btn btn-secondary">
        <i class="fa fa-reply"></i> Retour liste
    </a>
    
    <a href="{{ path('app_admin_' ~ entite ~ '_edit', {'id': entite.id}) }}" class="btn btn-primary">
        <i class="fa fa-edit"></i> Modification
    </a>
    {{ include('admin/_communs/_delete_form.html.twig') }}
</div>
