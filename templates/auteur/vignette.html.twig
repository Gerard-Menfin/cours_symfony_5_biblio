<div class="col-4 mb-3">
    <div class="card livre ">
        <h4 class="card-header bg-primary text-light">{{ auteur.identite }}</h4>

        <div class="row  couverture">
            {# <div class="col-4 d-flex align-items-center">
                <img class="card-img p-1 miniature" src="{{ auteur.photo }}" alt="pas de couverture">
            </div> #}

            <div class="col-12">
                <div class="card-body">
                    <p class="card-text">Livres : <strong>{{ auteur.livres|length }}</strong></p>
                    {# <p class="card-text">Mots-clés : <strong>{{ genre.motsCles }}</strong></p> #}
                </div>
            </div>
        </div>
        
        <div class="card-footer">
            <a href="{{ path("app_visiteur_genre_fiche", {libelle: genre.libelle}) }}" class="card-link"> 
                <i class="fa fa-book"></i> Voir plus 
            </a>
            </div>
    </div>
</div>



