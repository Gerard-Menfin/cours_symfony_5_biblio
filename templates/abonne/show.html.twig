{% extends 'base.html.twig' %}

{% block title %}Abonne{% endblock %}

{% block titre %}Fiche Abonne{% endblock %}

{% block body %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ abonne.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ abonne.pseudo }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ abonne.roles ? abonne.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ abonne.password }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ abonne.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ abonne.prenom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('abonne_index') }}">
<i class="fa fa-reply"></i> Retour liste
</a>

    <a href="{{ path('abonne_edit', {'id': abonne.id}) }}"><i class="fa fa-edit"></i></a>

    {{ include('abonne/_delete_form.html.twig') }}
{% endblock %}
