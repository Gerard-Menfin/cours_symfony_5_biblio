<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<a class="navbar-brand" href="{{ path("accueil") }}">
		<img src="{{ asset(chemin_images ~ "logo.png" )}}" alt="BiblioSF5" class="vignette">
	</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			{% if app.user %}
				{# si app.user est 'vrai', c'est qu'un utilisateur est connecté #}
				<li class="nav-item {{ app.request.attributes.get("_route") == "espace" ? "active" : "" }}">
					<a class="nav-link" href="{{ path("espace") }}">
						<i class="fa fa-user"></i>
						{{ app.user.username }}
					</a>
				</li>
				<li class="nav-item {{ app.request.attributes.get("_route") == "app_logout" ? "active" : "" }}">
					<a class="nav-link" href="{{ path("app_logout") }}">
						<i class="fa fa-sign-out-alt"></i>
						Déconnexion
					</a>
				</li>
			{% else %}
				<li class="nav-item {{ app.request.attributes.get("_route") == "app_login" ? "active" : "" }}">
					<a class="nav-link" href="{{ path("app_login") }}">
						<i class="fa fa-sign-in-alt"></i>
						Connexion
					</a>
				</li>

				<li class="nav-item {{ app.request.attributes.get("_route") == "app_register" ? "active" : "" }}">
					<a class="nav-link" href="{{ path("app_register") }}">
            			<i class="fa fa-user-plus"></i> Inscription
					</a>
				</li>
			{% endif %}

			{% if is_granted("ROLE_ADMIN") %}
				{{ include("_partiels/_lien_deroulant_menu.html.twig", { libelle: "Abonnés", liens: {"Liste": "abonne_index", "Ajouter": "abonne_new" }} ) }}
				{{ include("_partiels/_lien_deroulant_menu.html.twig", { 
					libelle: "Livres", 
					liens: {
						"Admin":			"",
						"divider1": 		"",
						"Liste": 			"admin_livre_index", 
						"Ajouter": 			"admin_livre_new",
						"divider2": 		"",
						"Ajouter (cat)": 	"admin_livre_new_cat",
						"divider3": 		"",
						"Livre Controller":	"",
						"divider4": 		"",
						"Liste_": 			"livre", 
						"Ajouter_": 		"livre_ajouter",
						"Nouveau_": 		"livre_nouveau"
					}} ) }}
				{% include "_partiels/_lien_deroulant_menu.html.twig" with {
					libelle: "Catégories",
					liens:  {"Liste": "admin_categorie_index", "Ajouter": "admin_categorie_new" }
				} %}
			{% endif %}

			{% if is_granted("ROLE_BIBLIO") %} {# is_granted permet de vérifier si l'utilisateur connecté à le rôle demandé #}
				{{ include("_partiels/_lien_deroulant_menu.html.twig", { libelle: "Emprunts", liens: {"Liste": "emprunt_index", "Ajouter": "emprunt_new" }} ) }}
			{% endif %}

		</ul>
		<form class="form-inline my-2 my-lg-0">
			<input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
			<button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
		</form>
	</div>
</nav>
